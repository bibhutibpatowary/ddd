let children =
  edgesData
    .filter((e: any) => {
      if (e.source !== node.id) return false;

      // 🔥 STRICT EXACT hierarchy match
      return (
        JSON.stringify(e.fullPath) ===
        JSON.stringify(activePath)
      );
    })
    .map((e: any) => e.target);
