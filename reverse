const hasReverseFocus = !!activeReverseLeaf;

const edgeOpacity = e.isReverse
  ? 1
  : hasReverseFocus
    ? 0.12
    : isPathEdge
      ? 1
      : 0.55;

style: {
  stroke: e.isReverse
    ? "#007bff"
    : isPathEdge
      ? "red"
      : branchColor,

  strokeWidth: e.isReverse
    ? 3.5
    : isPathEdge
      ? 3
      : 1.8,

  opacity: edgeOpacity,

  strokeDasharray: e.isReverse
    ? "5 5"
    : "0",

  transition: "all 0.25s ease"
}




=====

const hasReverseFocus = !!activeReverseLeaf;

const isInReversePath =
  selectedPath.includes(id);





opacity:
  hasReverseFocus
    ? isInReversePath
      ? 1
      : 0.25
    : 1
