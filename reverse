const visibleParents = parentMap[node.id] || [];

// 🔥 find the ACTUAL visible parent path
const matchedParentEdge = edgesData.find((e: any) => {
  return (
    e.target === node.id &&
    visibleParents.includes(e.source) &&
    visibleNodeIds.has(e.source)
  );
});

// 🔥 reconstruct correct hierarchy
const activePath = matchedParentEdge
  ? [...matchedParentEdge.fullPath, node.id]
  : [node.id];
