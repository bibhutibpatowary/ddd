const sortedEdges = [...edges].sort((a, b) => {
  // stable ordering by source then target
  const sourceCompare = a.source.localeCompare(b.source);

  if (sourceCompare !== 0) {
    return sourceCompare;
  }

  return a.target.localeCompare(b.target);
});
