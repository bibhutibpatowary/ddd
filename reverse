const [nodeActivePaths, setNodeActivePaths] = useState<any>({});

===


const initialPaths: any = {};

roots.forEach((r: string) => {
  initialPaths[r] = [r];
});

setNodeActivePaths(initialPaths);


===


const activePath =
  nodeActivePaths[node.id] || [node.id];

====

setNodeActivePaths((prev: any) => {
  const updatedPaths = { ...prev };

  children.forEach((childId: string) => {
    updatedPaths[childId] = [
      ...activePath,
      childId
    ];
  });

  return updatedPaths;
});

=====


const initialPaths: any = {};

roots.forEach((r: string) => {
  initialPaths[r] = [r];
});

setNodeActivePaths(initialPaths);

