const reverseHighlightedNodes = new Set<string>();

if (activeReverseLeaf) {
  const leafPaths = nodePaths[activeReverseLeaf] || [];

  leafPaths.forEach((path) => {
    path.forEach((nodeId) => {
      reverseHighlightedNodes.add(nodeId);
    });
  });
}

=====

const hasReverseFocus = !!activeReverseLeaf;

const isInReversePath =
  reverseHighlightedNodes.has(id);

opacity:
  hasReverseFocus
    ? isInReversePath
      ? 1
      : 0.2
    : 1
