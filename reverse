const possiblePaths =
  nodePaths[node.id] || [];

const activePath =
  possiblePaths.find((path) => {
    if (path.length === 1) return true;

    const parentId =
      path[path.length - 2];

    return visibleNodeIds.has(parentId);
  }) || possiblePaths[0];
